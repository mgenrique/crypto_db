# Example environment file for Crypto Portfolio Tracker
# Copy to `.env` and fill values (DO NOT commit `.env` with secrets)


# -----------------------------
# JWT / Auth / Security
# -----------------------------
# Use a long, random secret for production (min 32 chars)
SECRET_KEY=change_this_to_a_strong_secret_key_of_min_32_chars
# Token expiry configuration (integers)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------
# Exchanges / API keys
# -----------------------------
# Add API credentials for any exchange connectors you will enable
BINANCE_API_KEY=
BINANCE_API_SECRET=

COINBASE_API_KEY=
COINBASE_API_SECRET=
COINBASE_API_PASSPHRASE=

KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# NOTE: The app also supports storing per-user API keys via the UI/API

# -----------------------------
# Blockchain RPC endpoints (Alchemy/Infura/QuickNode, etc.)
# -----------------------------
# Used by connectors and networks.yaml interpolation (networks.rpc_url)
ETHEREUM_RPC_URL=
ARBITRUM_RPC_URL=
BASE_RPC_URL=
# Optional provider-specific keys (e.g. Alchemy/Infura)
# ALCHEMY_API_KEY=
# INFURA_PROJECT_ID=
# INFURA_PROJECT_SECRET=

# -----------------------------
# Third-party / Oracles
# -----------------------------
# CoinGecko is public; if you use a paid service add keys here
COINGECKO_API_KEY=
COINMARKETCAP_API_KEY=

# App/runtime settings moved to `config/config.yaml`:
# - `app.env` controls application environment (development/staging/production)
# - `logging.level` controls global logging level

# -----------------------------
# Optional integrations
# -----------------------------
# Sentry DSN for error tracking
SENTRY_DSN=

# Redis / cache (if used)
REDIS_URL=

# Email / SMTP (if sending emails)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# -----------------------------
# Notes
# -----------------------------
# - Copy this file to `.env` and populate secrets before running the app.
# - Never commit `.env` to version control.

# -----------------------------
# Wallet addresses (optional)
# -----------------------------
# ===== METAMASK - DIRECCIONES POR RED =====
# Formato: METAMASK_<NETWORK>=address
METAMASK_ETHEREUM=
METAMASK_ARBITRUM=
METAMASK_BASE=
METAMASK_POLYGON=
METAMASK_OPTIMISM=
METAMASK_AVALANCHE=

# ===== PHANTOM - DIRECCIONES POR RED =====
# Formato: PHANTOM_<NETWORK>=address
PHANTOM_SOLANA=
PHANTOM_ETHEREUM=
PHANTOM_POLYGON=
PHANTOM_BITCOIN=
